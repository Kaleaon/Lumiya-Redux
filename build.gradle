// Top-level build file. Per-module configuration lives in app/build.gradle.
plugins {
    id 'com.android.application' version '8.7.3' apply false
    id 'org.jetbrains.kotlin.android' version '1.9.25' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.layout.buildDirectory
}


subprojects {
    plugins.withId('org.jetbrains.kotlin.android') {
        kotlin {
            // Compile with whatever JDK Gradle is running on, while still
            // emitting Java 8 bytecode via module-level jvmTarget = "1.8".
            // JDK 17 is the minimum; JDK 21+ works too.
            jvmToolchain(Math.max(17, Integer.parseInt(System.getProperty('java.specification.version'))))
        }
    }
}
